# API

```@docs
@transform(x, args...)
```

```@docs
@select(x, args...)
```

```@docs
@combine(x, args...)
```

```@docs
@by(x, what, args...)
```

```@docs
@orderby(d, args...)
```

```@docs
@where(x, args...)
```

```@docs
@with(x, body)
```